import React, {Component} from "react";

export default class CustomerProfile extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <h1>Dashboard</h1>
        );
    }
}